interface:
  display_name: "Pi Spawner"
  short_description: "Delegate work to Pi model workers"
  default_prompt: "Use $pi-spawner to delegate specialist subtasks to read-by-default Pi workers with permissions, aliases, routes, and thinking levels."
